profiles {
    debug{
        cleanup = false
    }
    Hominidae {
        // a folder with bam files
        params.database  = "${projectDir}/assets/Hominidae/kallisto_index_k21.idx"  // a folder with the Kallisto database, Zavala2021-version provided in assets
        params.labels    = "${projectDir}/assets/Hominidae/labels.tsv"
    }
}